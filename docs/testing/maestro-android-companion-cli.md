# Maestro Android Companion CLI

This file is a compatibility pointer.

Use [`tools/maestro_android/README.md`](../../tools/maestro_android/README.md) as the canonical repo-local reference for Maestro companion CLI usage, lane mappings, and PocketGPT-specific setup.

If you maintain the standalone companion project separately, keep its install and release notes in that external repo rather than duplicating them here.
