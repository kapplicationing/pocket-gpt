appId: com.pocketagent.android
---
- runFlow: load-chat-last-used-if-present.yaml
- runFlow: retry-runtime-bootstrap.yaml
- runFlow: retry-runtime-bootstrap.yaml
- runFlow: retry-runtime-bootstrap.yaml
- runFlow: load-chat-last-used-if-present.yaml
- extendedWaitUntil:
    notVisible: "Setup"
    timeout: 180000
- extendedWaitUntil:
    notVisible: "Retry"
    timeout: 180000
- extendedWaitUntil:
    notVisible: "Loading…"
    timeout: 180000
- extendedWaitUntil:
    notVisible: "Loading..."
    timeout: 180000
- extendedWaitUntil:
    notVisible: "Cancel"
    timeout: 180000
- extendedWaitUntil:
    notVisible: "Cancelling"
    timeout: 180000
- extendedWaitUntil:
    notVisible: "Cancelling..."
    timeout: 180000
- extendedWaitUntil:
    notVisible: "Sending"
    timeout: 180000
- extendedWaitUntil:
    notVisible: "Update"
    timeout: 180000
- extendedWaitUntil:
    visible:
      id: "composer_input"
    timeout: 180000
- extendedWaitUntil:
    visible:
      id: "send_button"
    timeout: 180000
- extendedWaitUntil:
    notVisible:
      id: "chat_gate_inline_card"
    timeout: 180000
- extendedWaitUntil:
    notVisible:
      id: "chat_load_last_used_button"
    timeout: 180000
- assertNotVisible: "Setup"
- assertNotVisible: "Retry"
- assertNotVisible: "Loading…"
- assertNotVisible: "Loading..."
- assertNotVisible: "Cancel"
- assertNotVisible: "Cancelling"
- assertNotVisible: "Cancelling..."
- assertNotVisible: "Sending"
- assertNotVisible: "Update"
- assertNotVisible:
    id: "chat_gate_inline_card"
- assertNotVisible:
    id: "chat_load_last_used_button"
